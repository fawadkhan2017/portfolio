When updating the project:
- Always review `trickle/notes/README.md` to ensure it accurately reflects the current state of the application architecture, key components, and features.
- If new pages, major components, or dependencies are added, update the README to document these changes.